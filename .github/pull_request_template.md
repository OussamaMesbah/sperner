## What and why

<!-- What does this change, and why? Link the issue it closes, if any. -->

## Checklist

- [ ] The pull request targets `dev` (only release pull requests target `main`).
- [ ] `ruff format .`, `ruff check .` and `pytest` pass locally.
- [ ] New behaviour comes with a test.
- [ ] User-visible changes have an entry under "Unreleased" in CHANGELOG.md.
- [ ] If the algorithms changed: the benchmark was rerun and README.md shows its numbers.
